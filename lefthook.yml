pre-commit:
  parallel: false
  commands:
    fmt:
      run: scripts/lint.sh fmt
    fix:
      run: scripts/lint.sh run --fix

pre-push:
  commands:
    build:
      run: go build ./...
    test:
      run: go test -race ./...
    lint:
      run: scripts/lint.sh run
